# Lab 5 — Train a texture (outline)

Same loop as Lab 4, but the teacher is a texture sample. Compare memory footprint of the MLP weights vs the source image — compression / approximation intuition for neural textures (link RTXNTC as a production “next step,” don’t block on it).
