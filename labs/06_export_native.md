# Lab 6 — Export for native

Phase 1 already writes `SFMLP001`. Read:

- `docs/weight_format.md` — binary layout
- `native/README.md` — Falcor host that loads the same file
- `web/README.md` — browser demo that parses the same file

Checkpoint: you can explain bias/weight order to a teammate implementing `WeightLoader.cpp`.
